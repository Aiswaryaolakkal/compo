<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ConvertApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WOM"
        tools:targetApi="31">
        <activity
            android:name=".TVShowListActivity"
            android:exported="true"
            android:label="@string/title_activity_tvshow_list"
            android:theme="@style/Theme.WOM">

        </activity>
        <activity
            android:name=".LearningActivity2"
            android:exported="true"
            android:label="@string/title_activity_learning2"
            android:theme="@style/Theme.WOM">

        </activity>
        <activity
            android:name=".remember.RemeberActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".ui.theme.DetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ListingActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WOM">
            >
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.WOM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>